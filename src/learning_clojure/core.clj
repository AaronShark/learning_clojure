(ns learning-clojure.core
  (:require [learning-clojure.recipe1 :as lz77]
            [learning-clojure.recipe2 :as fractals]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Yes Clojure!"))
